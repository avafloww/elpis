Place an explicit model-local Jinja chat template here only when that model's
published tokenizer template fails `elpisbench-preflight`. ElpisBench does not
maintain a speculative registry of model-specific fixes.
