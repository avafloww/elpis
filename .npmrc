# openai@5's optional zod@^3 peer conflicts with @anthropic-ai/claude-agent-sdk's zod@^4.
# The lockfile resolution is correct; make plain `npm install` match it without a remembered flag.
legacy-peer-deps=true
